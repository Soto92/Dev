export function soma (a, b){
    return a + b;
}

export function div (a, b){
    return parseFloat(a) / parseFloat(b);
}

export function mult (a, b){
    return parseFloat(a) * parseFloat(b);
}

export function cubo (a, b){
    a= a*a*a;
    b= b*b*b;
    let arr=[a, b];
    return (arr);
}